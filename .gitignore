
# 【忽略文件】
#   secret.txt      = 忽略根目录下的 secret.txt
#   *.log           = 忽略所有 .log 文件（任意目录）
#   *.tmp           = 忽略所有 .tmp 文件
#
# 【忽略文件夹】
#   node_modules/   = 忽略 node_modules 文件夹（末尾加 / 表示目录）
#   dist/           = 忽略 dist 文件夹
#   .vscode/        = 忽略编辑器配置文件夹
#
# 【取消忽略（白名单）】
#   !important.log  = 即使 *.log 被忽略，也保留 important.log
#   !.gitignore     = 保留 .gitignore 本身不被忽略
#
# 【路径匹配】
#   /TODO           = 只忽略根目录的 TODO 文件（加 / 前缀限定根目录）
#   src/*.js        = 只忽略 src/ 下的 .js（不递归子目录）
#   src/**/*.js     = 忽略 src/ 下所有层级的 .js（** 匹配任意路径）
#   doc/*.txt       = 忽略 doc/ 目录下的所有 .txt
#
# =============================================

# 忽略所有文件（配合下方白名单使用）
*

# 白名单：明确要跟踪的文件/文件夹（在 * 之后用 ! 排除）
!.gitignore

# 忽略本地编辑器/工具配置（不需要提交到仓库）
.claude/
.vscode/
